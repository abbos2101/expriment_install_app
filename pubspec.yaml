name: expriment_install_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  ota_update: ^7.0.1

dev_dependencies:
  flutter_lints: ^5.0.0
  prompt_generator: ^0.0.4

flutter:
  uses-material-design: true